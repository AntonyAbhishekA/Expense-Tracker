# Copy this file to ".env" and fill in your real MySQL Workbench credentials.
# app.py automatically loads values from ".env" using python-dotenv.

DB_HOST=localhost
DB_USER=root
DB_PASSWORD=root
DB_NAME=expense_tracker
DB_PORT=3306
